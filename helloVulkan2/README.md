# Problem shooting


### When Creating std::hash for a user-defined Class, it's not compatible
> for the struct
> ```cpp
> struct Vertex
> {
>    glm::vec3 pos;
>    glm::vec3 color;
>    glm::vec2 tex_coord;
>}
> ```
